{{- template "env.nu" . -}}

$env.mac_path = [
	"/opt/homebrew/bin",
]

$env.mac_path | where $it not-in $env.PATH | each { |path|
	$env.PATH | split row (char esep) | prepend path
}

$env.ASDF_NU_DIR = (brew --prefix asdf | into string | str trim | path join 'libexec')
$env.LC_ALL = "en_US.UTF-8"
